import { Open_Sans } from "next/font/google";

export const OpenSansLight = Open_Sans({
	display: "swap",
	weight: "300",
	subsets: ["latin"],
	variable: "--font-open-sans-light",
});

export const OpenSansRegular = Open_Sans({
	display: "swap",
	weight: "400",
	subsets: ["latin"],
	variable: "--font-open-sans-regular",
});

export const OpenSansMedium = Open_Sans({
	display: "swap",
	weight: "500",
	subsets: ["latin"],
	variable: "--font-open-sans-medium",
});

export const OpenSansSemiBold = Open_Sans({
	display: "swap",
	weight: "600",
	subsets: ["latin"],
	variable: "--font-open-sans-semibold",
});

export const OpenSansBold = Open_Sans({
	display: "swap",
	weight: "700",
	subsets: ["latin"],
	variable: "--font-open-sans-bold",
});

export const OpenSansExtraBold = Open_Sans({
	display: "swap",
	weight: "800",
	subsets: ["latin"],
	variable: "--font-open-sans-extrabold",
});
